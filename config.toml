baseURL = "https://www.thekitty.zone/"
languageCode = "en-us"
title = "The kitty zone"
# How many feed entries to load at once
paginate = 6

[taxonomies]
tag = "tags"

[author]
name = "89Q12"
image = "images/img.png"
about = "Is this... Is this really real? Where are all the kittens and cats if this is the kitty zone?"

[params]
skin = "macintosh"
mainSections = "posts"
#
# For IndieAuth and the author card
#
githubuser = "89Q12"
mastodon = "https://catcatnya.com/@89Q12" # full link to your instance profile

[permalinks]
# Format for feed entry links. You probably don't need to change this.
feed = "/:title/"

[outputs]
# Output formats for your feed. You probably don't need to change this.
home = ["HTML", "JSON", "RSS"]
